# notes_app_using_django

### Getting Started
Install the required packages and libraries using the following command ```pip install -r requirements.txt```
Run the following command ```python manage.py runserver``` 
